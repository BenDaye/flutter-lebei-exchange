import 'package:flutter/material.dart';

class PageTrade extends StatelessWidget {
  const PageTrade({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
